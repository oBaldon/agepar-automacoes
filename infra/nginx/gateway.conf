server {
  listen 80;
  server_name _;

  # Placeholder: só para confirmar que o gateway está no ar.
  location / {
    return 200 "Gateway placeholder. Configure no Dia 5.\n";
    add_header Content-Type text/plain;
  }
}
